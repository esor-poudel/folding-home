<Window x:Class="DesktopClient.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Peer-to-Peer Demo" Height="350" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter Python Job Code:" FontWeight="Bold"/>
        <TextBox x:Name="JobInputBox" Grid.Row="1" Height="100" Margin="0,5,0,5"
                 TextWrapping="Wrap" AcceptsReturn="True"/>

        <Button x:Name="SubmitButton" Grid.Row="2" Content="Submit Job" Height="30"
                Click="SubmitButton_Click"/>

        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="0,10,0,0"
                   Text="No jobs yet." FontStyle="Italic" Foreground="Gray"/>
    </Grid>
</Window>